export enum UserType {
  STUDENT = 'student',
  TEACHER = 'teacher',
}

export enum StudentLevel {
  PRO = 'PRO',
  A = 'A',
  B = 'B',
  C = 'C',
  D = 'D',
}

export enum PaymentStatus {
  PAID = 'paid',
  PENDING = 'pending',
}

export enum PaymentValue {
  INDIVIDUAL1X = 180,
  INDIVIDUAL2X = 240,
  INDIVIDUAL3X = 320,
}
