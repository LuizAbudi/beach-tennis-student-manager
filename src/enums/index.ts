export enum UserType {
  STUDENT = 'student',
  TEACHER = 'teacher',
}

export enum LevelNames {
  PRO = 'PRO',
  A = 'A',
  B = 'B',
  C = 'C',
  D = 'D',
}

export enum PaymentStatus {
  PAID = 'paid',
  PENDING = 'pending',
  EXPIRED = 'expired',
}
